# Ignore electron-winstaller native build (not needed on macOS/Linux)
ignore-dep-scripts[electron-winstaller]=true

# Only build these native dependencies
only-built-dependencies[]=better-sqlite3
only-built-dependencies[]=electron
only-built-dependencies[]=esbuild
